// Re-exported from @tracker-engine/platform; shim so '@/lib/platform' importers
// don't reach into the package directly.
export {
  haptic,
  hapticSuccess,
  isNativePlatform,
  platform,
} from '@tracker-engine/platform'
