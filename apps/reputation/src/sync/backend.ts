// Re-exported from @tracker-engine/local-first (extracted in the monorepo split — see
// docs/design-expense-tracker.md). Kept as a shim so existing '@/sync/backend'
// importers don't change.
export type { PulledRow, PushOutcome, PushRow, SyncBackend } from '@tracker-engine/local-first'
