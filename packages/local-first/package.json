{
  "name": "@tracker-engine/local-first",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Domain-agnostic local-first sync: outbox drain, delta pull, dead-letter, backoff, and a Supabase backend. Driven by an injected SyncSchema + SyncDeps.",
  "exports": {
    ".": "./src/index.ts",
    "./columnCase": "./src/columnCase.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.1"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
