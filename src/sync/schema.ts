// Re-exported from @tracker-engine/local-first; shim so existing '@/sync/schema' importers
// (repSchema, engine, tests) don't change.
export type { SyncRowStore, SyncSchema } from '@tracker-engine/local-first'
